{
  "name": "aurore-static-vite",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "predev": "node scripts/generate-api-pages.mjs",
    "dev": "vite --host 127.0.0.1 --port 5178",
    "dev:host": "vite --host 0.0.0.0 --port 5178",
    "prebuild": "node scripts/generate-api-pages.mjs",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1 --port 5178"
  },
  "devDependencies": {
    "vite": "^6.3.5"
  }
}
